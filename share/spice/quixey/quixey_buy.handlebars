<span class="detail__callout">
{{#each editions}}
    <a href="{{{url}}}" class="btn">
        {{#with this.platforms.[0]}}
        <img src="{{platform_icon icon_url}}" class="btn__icon  platform_icon">
        {{/with}}
        {{platform_name}}
        {{#if ../hasPricerange}}
             - {{qprice cents}}
        {{/if}}
    </a>
{{/each}}
</span>
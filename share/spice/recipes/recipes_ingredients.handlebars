{{#if ingredientDetails}}
	<div class="detail__ingredients">
		Ingredients: 
		<ul>
			{{#each ingredientDetails}}
				<li><a href="{{url}}">{{displayName}}</a></li>
			{{/each}}
		</ul>
	</div>
{{else}}
	&nbsp;
{{/if}}
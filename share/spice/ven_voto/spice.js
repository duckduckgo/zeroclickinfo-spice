function ddg_spice_ven_voto(ir) {
        items = new Array();
        items[0] = new Array();

        items[0]['a'] = ir;
        items[0]['h'] = '';
        items[0]['s'] = 'CNE';
        items[0]['u'] = 'http://www.cne.gob.ve/';

        nra(items);
}
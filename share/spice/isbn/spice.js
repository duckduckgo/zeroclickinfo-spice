function ddg_spice_isbn(response) {
    alert('test');
    console.log(response);
	var items = new Array();
	items[0] = new Array();
    items[0]['a'] = 'success';
	items[0]['h'] = "Rhymes with " + query;
	items[0]['s'] = 'RhymeBrain';
	items[0]['u'] = 'http://rhymebrain.com/';
	
	nra(items);
}

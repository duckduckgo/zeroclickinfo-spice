{{#if platforms}}
    <span class="detail__callout">
        Available On:  
        {{#each platforms}}
            <a href="{{{site_detail_url}}}" class="btn">
                {{name}}
            </a>
        {{/each}}
    </span>
{{/if}}
function ddg_spice_automeme(memes) {
    if (memes.length) {
        meme = memes[0];
        items = new Array();
        items[0] = new Array();
        items[0]['a'] = meme;
        items[0]['h'] = '';
        items[0]['s'] = 'Automeme';
        items[0]['u'] = 'http://autome.me/';
        nra(items);
    }
}

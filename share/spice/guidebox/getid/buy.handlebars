<div>
  <p>
    <a class="btn btn--primary" href="{{smart_url}}">Watch Now</a>
    {{#guideBox_checkSeason season_number episode_number}}
      {{{episode_name}}} (Season {{season_number}}, #{{episode_number}})
    {{/guideBox_checkSeason}}
    {{#unless is_free_web}} <span class="red">Paid Content</span>{{/unless}}
  </p>

  <p>
    {{overview}}
  </p>
    
  <i class="small grey">Originally aired {{guideBox_getDate first_aired}} on {{guideBox_getNetwork}}</i>
</div>
function ddg_spice_rand_nums(ir) {
	items = new Array();
	items[0] = new Array();

	items[0]['a'] = ir;
	items[0]['h'] = '';
	items[0]['s'] = 'Random.org';
	items[0]['u'] = 'http://random.org/';

	nra(items);
}

function ddg_spice_song_kick(result) {

    if ( ) {
        items = [[]];
        
        items[0]['a'] = ;
        items[0]['h'] = ;
        items[0]['s'] = ;
        items[0]['u'] = ;
        items[0]['f'] = ;
        // items[0]['force_big_header'] = true;
        // items[0]['force_space_after'] = true;

        // Render
        nra(items);
    }
}
---
# Template definitions for Spice IAs.

# Each template represents one input and one output file.
# Modification of files generated by certain templates will need to restart a
# running duckpan instance in order to be recognized:
#   - Perl modules
# This is indicated by the 'needs_restart' field.
templates:
    pm:
        label:  Perl Module
        input:  lib/DDG/Spice/Example.pm
        output: lib/DDG/Spice/<:$ia_path:>.pm
        needs_restart: true

    test:
        label:  Perl Module Test
        input:  t/Example.t
        output: t/<:$ia_path:>.t

    js:
        label:  Javascript
        input:  share/spice/example/example.js
        output: share/spice/<:$ia_path_lc:>/<:$ia_id:>.js

    css:
        label:  CSS
        input:  share/spice/example/example.css
        output: share/spice/<:$ia_path_lc:>/<:$ia_id:>.css

    handlebars:
        label:  Handlebars
        input:  share/spice/example/example.handlebars
        output: share/spice/<:$ia_path_lc:>/<:$ia_id:>.handlebars


# A template set is a collection of templates used to generate all files of a
# sub-type of an Instant Answer type. Each set can have some 'required'
# templates (which are always applied) and some 'optional' templates (which are
# applied after asking the user).
template_sets:
    default:
        description: Standard Spice Instant Answer
        required: [ pm, test, js ]
        optional: [ css, handlebars  ]

    all:
        description: Spice with all backend and frontend files
        required: [ pm, test, js, css, handlebars ]
...

